{
	"name": "omp-classifier",
	"version": "0.2.1",
	"description": "Model-judged permission gate for OMP: intercepts bash calls and spawn-bearing eval payloads the host would auto-approve, classifies them, and prompts on anything risky instead of running it silently.",
	"type": "module",
	"private": true,
	"scripts": {
		"test": "bun test",
		"typecheck": "bunx tsc -p tsconfig.json"
	},
	"engines": {
		"bun": ">=1.3.14"
	},
	"omp": {
		"extensions": [
			"./index.ts"
		]
	},
	"devDependencies": {
		"@oh-my-pi/pi-ai": "17.3.8",
		"@oh-my-pi/pi-coding-agent": "17.3.8",
		"@oh-my-pi/pi-utils": "17.3.8",
		"@types/bun": "^1.3.14",
		"typescript": "^7.0.2"
	}
}
